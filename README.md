# Predictive-Maintenance-System
The system collects vehicle sensor data (engine temp, RPM, vibration, oil pressure, etc.), runs it through an ML model, and predicts which component is likely to fail — and when — so maintenance can be scheduled proactively.
